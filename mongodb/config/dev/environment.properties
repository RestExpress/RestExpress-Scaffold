# Default is 8081
port = 8081

# The size of the executor thread pool (that can handle blocking back-end processing).
executor.threadPool.size = 20

# The default service response format (default is JSON)
#default.format = json

# A comma-delimited string of MongoDB server hosts
mongodb.bootstraps = localhost:27017
mongodb.database = scaffold
#mongodb.user =
#mongodb.password =
#mongodb.connectionsPerHost = 100
#mongodb.useKeepAlive = false
#mongodb.connectTimeoutMS = 10000
#mongodb.autoConnectRetry = false
#mongodb.maxAutoConnectRetryTimeMS = 0
#mongodb.maxWaitTimeMS = 120000
#mongodb.socketTimeoutMS = 0

# The base URL, used as a prefix for links returned in data
# default is http://localhost:<port>
#base.url = http://localhost:8081